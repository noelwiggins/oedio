return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )—return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )1return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )9return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )2return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )9return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )zreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    );return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    );return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )+return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )1return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )+return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )1return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Qreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )8return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )2return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )2return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )8return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )8return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )3return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )8return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )<return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )<return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )<return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )!return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )[return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )]return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )'return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )<return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )>return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )!return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )#return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )-return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )xreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )4return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )wreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )creturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )jreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )yreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )@return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )/return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )zreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )zreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ){return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )lreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Kreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )}return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )freturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )mreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )_return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ):return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )areturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )hreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )sreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )vreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ireturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )nreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ).return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )(return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Preturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Oreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )"return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )5return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )0return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ),return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ) return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )dreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )breturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )greturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )=return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )Treturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )rreturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ureturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )ereturn render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    ))return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )
return render_template(
        "index.html",
        megabooks=MEGABOOKS,
        sections=SECTIONS,
        megabooks_by_section={
            s["slug"]: [mb for mb in MEGABOOKS if mb.get("section") == s["slug"]]
            for s in SECTIONS
        },
        total_megabooks=len(MEGABOOKS),
        total_components=sum(len(mb.get("components",[])) for mb in MEGABOOKS),
    )